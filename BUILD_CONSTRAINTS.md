# 全局构建约束

本文用于约束 AI HMP 项目的前端构建与界面实现。后续新增页面、组件和样式应优先遵守本文规则。

## UI 组件

1. 医护端（B 端）前端统一优先使用 antd 规范与组件。
2. 只有在 antd 组件或规范无法满足明确业务、交互或视觉需求时，才自行定义规范或组件。
3. 自定义组件应尽量复用 antd 的设计 token、交互状态和可访问性模式，避免形成独立体系。

## 圆角

1. 在没有特别要求的情况下，antd 组件统一保留 antd 默认圆角，不额外覆盖 `border-radius`。
2. 自定义容器或自定义交互元素如需圆角，应优先对齐 antd 默认圆角 token，除非产品稿或业务场景明确要求差异化。
3. 如确需自定义圆角，应在对应需求、设计说明或代码注释中说明原因。
