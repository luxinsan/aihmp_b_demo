# 平台管理端构建计划

## 1. 目标

新建平台管理端（M 端）独立应用目录，用于承载健康管理平台的平台级租户管理、模块配置等底层治理能力。

本次完成 M 端基础框架、租户管理列表页，以及租户维度的模块配置页面。

## 2. 技术栈

平台管理端采用新的前端技术栈：

- Vue 3
- TypeScript
- Vite
- Ant Design Vue
- Vue Router
- CSS

后续医护端（B 端）如果统一迁移到 Vue 3 + Ant Design Vue，平台管理端可作为新规范参考。

## 3. 端侧定位

| 端侧 | 名称 | 说明 |
| --- | --- | --- |
| M 端 | 平台管理端 | 面向平台管理、实施、运营人员，负责租户、平台资源、平台配置、运营审计和平台级治理 |
| B 端 | 医护端 | 面向医生、护士、健康管理师、机构管理者等租户侧业务人员 |
| C 端 | 患者端 | 面向患者、居民、健康管理服务对象 |

平台管理端应与医护端、患者端保持产品边界清晰，不混用端侧职责。

## 4. 应用目录

计划新增目录：

```text
platform-admin/
```

建议目录结构：

```text
platform-admin/
├─ index.html
├─ package.json
├─ tsconfig.json
├─ tsconfig.app.json
├─ tsconfig.node.json
├─ vite.config.ts
└─ src/
   ├─ main.ts
   ├─ App.vue
   ├─ styles.css
   ├─ router/
   │  └─ index.ts
   ├─ data/
   │  ├─ tenants.ts
   │  └─ moduleConfig.ts
   ├─ layouts/
   │  └─ PlatformShell.vue
   └─ pages/
      ├─ TenantListPage.vue
      └─ ModuleConfigPage.vue
```

## 5. 页面范围

### 5.1 应用壳

平台管理端基础布局：

- 左侧侧边菜单。
- 顶部栏。
- 内容区。
- 使用 Ant Design Vue 的 Layout、Menu、Table、Tag、Button 等基础组件。

### 5.2 侧边菜单

当前只提供一个菜单：

```text
租户管理
```

菜单对应租户管理列表页。

### 5.3 租户管理列表页

页面用于展示租户 mock 数据，并提供模块配置入口。

表格字段：

| 字段 | 说明 |
| --- | --- |
| 租户名称 | 租户展示名称 |
| 租户编码 | 租户稳定编码 |
| 租户类型 | 医院 / 健康管理机构 / 试点机构等 |
| 状态 | 启用 / 停用 |
| 已启用模块数 | 模块配置概览 |
| 更新时间 | 最近更新时间 |
| 操作 | 模块配置入口 |

### 5.4 模块配置页

页面用于按租户配置服务模块下的能力点。

| 区域 | 说明 |
| --- | --- |
| 页面标题 | 展示“模块配置”和当前租户名称 |
| 操作区 | 返回、保存 |
| 配置表格 | 按服务模块分组展示能力点 |
| 能力点行 | 展示能力点名称、能力点 key、对应权限点 key、说明、复选框 |

### 5.5 Mock 数据

先使用本地 mock 数据，不接真实接口。

数据文件：

```text
platform-admin/src/data/tenants.ts
platform-admin/src/data/moduleConfig.ts
```

## 6. 路由规划

| 路径 | 页面 |
| --- | --- |
| `/` | 重定向到 `/tenants` |
| `/tenants` | 租户管理列表页 |
| `/tenants/:tenantId/module-config` | 租户模块配置页 |

## 7. 视觉与交互规范

- 页面规范参考现有医护端（B 端）后台系统风格。
- 使用 Ant Design Vue 组件体系。
- 保持左侧导航 + 顶部栏 + 内容区的后台系统布局。
- 信息密度适中，优先清晰、稳定、易扫描。
- 不做营销式首页、欢迎页或大面积装饰设计。
- 文案遵守 `TERMINOLOGY.md`，正式产品名称使用“健康管理平台”。

## 8. 暂不包含

当前暂不包含：

- 登录认证。
- 真实接口请求。
- 权限控制。
- 租户详情页。
- 真实模块配置保存接口。
- 与医护端（B 端）代码迁移联动。

## 9. 构建任务清单

| 状态 | 任务 | 说明 |
| --- | --- | --- |
| 已完成 | 创建 `platform-admin` 应用目录 | 新建 Vue 3 + TypeScript + Vite 应用结构 |
| 已完成 | 安装并配置 Ant Design Vue | 接入基础组件和样式 |
| 已完成 | 配置 Vue Router | 实现 `/` 到 `/tenants` 的路由 |
| 已完成 | 创建平台管理端应用壳 | 左侧菜单、顶部栏、内容区 |
| 已完成 | 创建租户 mock 数据 | 本地维护租户列表数据 |
| 已完成 | 创建租户管理列表页 | 表格展示 mock 租户数据 |
| 已完成 | 添加模块配置入口 | 操作列展示“模块配置”按钮，并跳转到租户配置页 |
| 已完成 | 创建模块配置 mock 数据 | 本地维护服务模块与能力点数据 |
| 已完成 | 创建租户模块配置页 | 按服务模块分组展示能力点，支持复选框和保存反馈 |
| 已完成 | 本地构建验证 | 运行构建，确认无 TypeScript / Vite 错误 |
| 已完成 | 本地页面验证 | 启动 dev server，检查页面基础展示 |

## 10. 验收标准

- 项目根目录下存在独立 `platform-admin` 应用。
- 应用可使用 Vue 3 + TypeScript + Vite 启动。
- Ant Design Vue 组件可正常渲染。
- 左侧菜单只包含“租户管理”。
- `/tenants` 可展示租户管理列表页。
- 租户数据来自本地 mock。
- 操作列存在“模块配置”入口，并可进入租户模块配置页。
- 模块配置页按“服务模块 - 能力点”展示配置项。
- 能力点展示能力点 key、对应权限点 key、说明和复选框。
- 构建命令可通过。
